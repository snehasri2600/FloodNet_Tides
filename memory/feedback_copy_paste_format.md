---
name: feedback-copy-paste-format
description: User wants code delivered as plain copy-paste text blocks, not inserted into files via tools
metadata:
  type: feedback
---

Always deliver code as plain markdown code blocks (copy-paste format) rather than using NotebookEdit or other file-editing tools to insert code automatically.

**Why:** User explicitly requested this format for the session and wants to paste code themselves.

**How to apply:** For the rest of this session (and future sessions), output code in fenced code blocks instead of auto-inserting via NotebookEdit or Edit tools.
